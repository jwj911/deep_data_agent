# 验收清单

- [x] 项目分析记录审计日期、当前分支、完整 HEAD SHA 和工作区状态，且信息来自本轮实际命令。
- [x] `.trae/specs/` 下全部既有 change-id 的完成状态已核对，候选规划不重复已完成工作。
- [x] Python 3.12 环境下 pytest、isort、发布契约、Alembic 单 head、Compose 解析和 `git diff --check` 的实际结果已记录。
- [x] 前端 typecheck、零警告 lint、format:check 和 build 的实际结果已记录，构建产物已清理。
- [x] Hosted CI、Docker 五服务、模型/搜索等无法取得的证据被明确标为缺口，没有以历史结果或静态检查冒充通过。
- [x] 审计覆盖 FastAPI、LangGraph、服务、数据库、Redis、前端之间的真实调用链、生命周期和配置边界。
- [x] 审计覆盖认证、RBAC、会话所有权、CORS、限流、审计日志、凭据脱敏和管理员操作。
- [x] 审计覆盖模型/搜索调用、缓存键、文件分析、代码执行开关、临时文件、资源限制和外部输入边界。
- [x] 审计覆盖前端登录态、REST/LangGraph 分离、流式线程、文件上传、图表路径、静态导出和浏览器公开配置。
- [x] 审计覆盖 Docker Compose、GitHub Actions、依赖可重复性、备份恢复、发布契约和运行环境约束。
- [x] `utils/` 被作为独立遗留边界审查，文档明确其是否进入主应用、是否受发布门禁覆盖及后续处置。
- [x] 每个最终问题具有唯一、稳定的审计 ID，且不存在重复 ID 或同一问题多次记录。
- [x] P0-P3 严重度定义一致应用，严重度与实际用户影响、可利用性和恢复成本匹配。
- [x] 每个问题提供最小且可点击的新版本文件/行号证据，单个证据范围不超过 100 行。
- [x] 每个问题包含影响、建议、依赖和可验证的完成条件，结论不止描述现状。
- [x] 纯风格、无证据猜测、重复问题和不具行动价值的低价值评论已排除。
- [x] 两个独立验证者分别复核全部候选问题，并返回存在性、严重度和理由。
- [x] 最终问题按 2/2、1/2、0/2 共识标记置信度；0/2 误报被排除，1/2 项带 caveat。
- [x] 最终问题表按 P0→P3、阻塞关系和用户影响排序；开放决策与确定问题分开记录。
- [x] `project_analysis.md` 按范围与证据、当前架构、已验证能力、问题清单、开放决策和发布判断组织，旧描述无冲突。
- [x] 项目分析包含真实请求/身份/数据/缓存/Agent/工具流程 Mermaid 图，强调节点同时设置高对比度填充色和文字色。
- [x] Roadmap 保留已完成里程碑，并将后续工作划分为近期阻塞项、中期业务能力和长期平台候选；每轮具备依赖、进入条件、范围、非目标、验收门槛和停止条件。
- [x] Roadmap 包含迭代依赖 Mermaid 图，清楚标识串行阻塞、可并行项和数据分析/报告能力的解锁路径，并满足颜色对比要求。
- [x] 每个有效问题已映射到后续迭代、延期理由或风险接受决策；文件/租户/资源/隔离前置条件未满足时，数据分析候选不会被提前。
- [x] `project_analysis.md`、`roadmap.md`、`CHANGELOG.md` 及必要的 README/AGENTS 状态日期、SHA、验证数字和迭代名称一致；发布契约与 `git diff --check` 通过，文档不含敏感原文且本轮无运行时代码变更。
